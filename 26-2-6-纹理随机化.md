在isaaclab_logistics_vla/tasks/mdp/events.py中新增了两个函数
1.add_text_to_image在指定图片的指定区域内添加文字，并保存为新图片
2.set_package_visual_texture将环境中的包裹资产随机化为指定纹理，首先根据传来的参数生成对应图片再将图片作为指定纹理，
    注意：project_uvw=False  # 强制关闭 UV 投影，使用默认 UV