# 多人协作开发指南

基于自动注册机制，多人可以并行开发各自的任务，互不干扰，最终通过统一的入口脚本使用。

---

## 🏗️ 协作架构

### 整体结构

```
isaaclab_logistics_vla/
├── isaaclab_logistics_vla/
│   ├── __init__.py              # 自动注册所有任务（无需修改）
│   ├── tasks/
│   │   ├── __init__.py          # 可选：显式导入（推荐用于文档）
│   │   ├── realman_lift/        # 开发者A的任务
│   │   ├── single_arm_sorting/  # 开发者B的任务
│   │   ├── my_new_task/         # 开发者C的新任务
│   │   └── ...
│   └── utils/
│       └── importer.py          # 自动导入工具（无需修改）
├── random_agent.py              # 统一入口脚本（无需修改）
└── setup.py                     # 包配置
```

### 关键设计优势

✅ **模块化**：每个任务独立目录，互不干扰  
✅ **自动化**：`import_packages()` 自动发现所有任务  
✅ **统一接口**：所有任务通过相同的 API 使用  
✅ **易于扩展**：添加新任务只需创建新目录  

---

## 👥 多人协作流程

### 第一步：克隆仓库

```bash
# 每个人克隆主仓库
git clone https://github.com/your-org/isaaclab_logistics_vla.git
cd isaaclab_logistics_vla
pip install -e .
```

### 第二步：创建自己的任务分支

```bash
# 开发者A：创建自己的任务分支
git checkout -b feature/realman-lift

# 开发者B：创建自己的任务分支
git checkout -b feature/franka-sorting

# 开发者C：创建自己的任务分支
git checkout -b feature/new-task
```

### 第三步：开发自己的任务

每个开发者在自己的分支上创建任务目录：

```
# 开发者A的工作
tasks/realman_lift/
├── __init__.py              # 注册 gym.register("Isaac-Realman-lift", ...)
├── realman_lift_env_cfg.py  # 环境配置
└── mdp/
    └── __init__.py          # MDP 函数

# 开发者B的工作
tasks/single_arm_sorting/
├── __init__.py
├── sorting_env_cfg.py
└── mdp/
    └── __init__.py

# 开发者C的工作
tasks/my_new_task/
├── __init__.py
├── new_task_env_cfg.py
└── mdp/
    └── __init__.py
```

### 第四步：提交和合并

```bash
# 开发者A提交
git add tasks/realman_lift/
git commit -m "Add Realman lift task"
git push origin feature/realman-lift

# 通过 Pull Request 合并到主分支
# 其他开发者拉取更新
git pull origin main
```

---

## 📝 开发规范

### 1. 任务目录结构规范

每个任务必须遵循标准结构：

```
tasks/your_task_name/
├── __init__.py              # 必需：注册 Gym 环境
├── your_task_env_cfg.py     # 必需：环境配置类
├── mdp/
│   └── __init__.py          # 必需：MDP 函数定义
├── assets/                  # 可选：任务特定的资产
└── README.md                # 可选：任务说明文档
```

### 2. 任务注册规范

`tasks/your_task_name/__init__.py` 必须包含：

```python
import gymnasium as gym

gym.register(
    id="Isaac-YourTaskName-v0",  # 任务ID：统一格式
    entry_point="isaaclab.envs:ManagerBasedRLEnv",
    disable_env_checker=True,
    kwargs={
        "env_cfg_entry_point": f"{__name__}.your_task_env_cfg:YourTaskEnvCfg",
        # 可选：训练配置
        # "rsl_rl_cfg_entry_point": f"{agents.__name__}.rsl_rl_ppo_cfg:YourTaskPPORunnerCfg",
    },
)
```

**命名规范**：
- 任务ID：`Isaac-<TaskName>-v<Version>`
- 例如：`Isaac-Realman-lift`, `Isaac-Logistics-SingleArmSorting-Franka-v0`

### 3. 配置类规范

`your_task_env_cfg.py` 必须基于 `ManagerBasedRLEnvCfg`：

```python
from isaaclab.envs import ManagerBasedRLEnvCfg
from isaaclab.utils import configclass

@configclass
class YourTaskEnvCfg(ManagerBasedRLEnvCfg):
    """任务配置类"""
    
    # 场景配置
    scene: YourSceneCfg = YourSceneCfg(num_envs=2, env_spacing=2.0)
    
    # MDP 配置
    observations: ObservationsCfg = ObservationsCfg()
    actions: ActionsCfg = ActionsCfg()
    rewards: RewardsCfg = RewardsCfg()
    # ...
    
    def __post_init__(self):
        """后处理配置"""
        self.decimation = 2
        self.episode_length_s = 5.0
        # ...
```

### 4. 依赖管理规范

**公共依赖**：放在项目根目录的 `requirements.txt` 或 `setup.py` 中

```python
# setup.py
install_requires=[
    "isaaclab",
    "gymnasium",
    "torch",
    # ... 公共依赖
]
```

**任务特定依赖**：在任务目录中创建 `requirements.txt`

```
# tasks/your_task/requirements.txt
# 任务特定的依赖（如果有）
some-specific-package>=1.0.0
```

### 5. 资产管理规范

**共享资产**：放在公共目录

```
isaaclab_logistics_vla/
└── assets/
    ├── robots/          # 共享机器人模型
    └── objects/         # 共享物体模型
```

**任务特定资产**：放在任务目录

```
tasks/your_task/
└── assets/
    └── task-specific-objects/
```

---

## 🔄 Git 工作流程建议

### 方案 A：分支策略（推荐）

```bash
main                    # 主分支（稳定版本）
├── feature/realman-lift
├── feature/franka-sorting
├── feature/new-task
└── develop             # 开发分支（集成测试）
```

**流程**：
1. 从 `main` 创建功能分支
2. 在功能分支上开发
3. 完成开发后，创建 Pull Request 合并到 `develop`
4. 测试通过后，合并到 `main`

### 方案 B：任务目录隔离

```bash
# 每个人在独立的任务目录中工作，避免冲突
tasks/
├── developer_a/        # 开发者A的所有任务
│   ├── task1/
│   └── task2/
├── developer_b/        # 开发者B的所有任务
│   └── task3/
└── shared/             # 共享任务
    └── common_task/
```

---

## ⚠️ 注意事项和最佳实践

### 1. 避免修改共享文件

**不要修改**：
- ❌ `isaaclab_logistics_vla/__init__.py`（除非添加新的公共功能）
- ❌ `utils/importer.py`（自动导入工具）
- ❌ `random_agent.py`（统一入口脚本，除非是公共改进）

**可以修改**：
- ✅ 自己的任务目录
- ✅ 任务文档
- ✅ 通过 Pull Request 改进共享代码

### 2. 任务命名冲突

**问题**：多个开发者可能使用相同的任务名称

**解决方案**：
- 使用清晰的任务ID（包含开发者或项目前缀）
- 例如：`Isaac-Logistics-RealmanLift-v0` 而不是 `Isaac-Lift-v0`
- 在团队中维护任务名称列表

### 3. 显式导入 vs 自动导入

**选项A：纯自动导入（推荐用于大型项目）**

```python
# tasks/__init__.py
# 完全依赖 import_packages() 自动发现
# 无需手动维护列表
```

**选项B：显式导入（推荐用于小型团队）**

```python
# tasks/__init__.py
from . import realman_lift  # noqa: F401
from . import single_arm_sorting  # noqa: F401
# 明确列出所有任务，作为文档
```

**建议**：
- 小型团队（<5人）：使用显式导入，更清晰
- 大型团队（>5人）：使用纯自动导入，更灵活

### 4. 冲突处理

**场景**：两个开发者同时修改了 `tasks/__init__.py`

**解决方案**：
- 如果使用显式导入，合并时需要手动解决冲突
- 如果使用纯自动导入，不会冲突（无需修改 `tasks/__init__.py`）

### 5. 测试和验证

**每个开发者应该**：
1. 本地测试自己的任务
2. 确保任务可以注册和运行
3. 使用统一入口脚本测试视频录制

```bash
# 开发者A测试自己的任务
python random_agent.py \
    --task Isaac-Realman-lift \
    --num_envs 1 \
    --headless \
    --record-video test.mp4
```

### 6. 文档规范

**每个任务应该包含**：
- `README.md`：任务说明、使用方法、参数说明
- 代码注释：关键配置和函数说明

```
tasks/your_task/
├── README.md              # 任务说明文档
├── __init__.py
├── your_task_env_cfg.py
└── mdp/
    └── __init__.py
```

**README.md 模板**：

```markdown
# Your Task Name

## 任务描述
简要描述任务的目标和功能。

## 使用方法

```bash
python random_agent.py \
    --task Isaac-YourTaskName-v0 \
    --num_envs 2 \
    --headless \
    --record-video output.mp4
```

## 配置说明
- `num_envs`: 环境数量（默认：2）
- `episode_length_s`: 回合时长（默认：5.0秒）
- ...

## 依赖
- 特定依赖列表（如果有）

## 开发者
- 开发者姓名/联系方式
```

---

## 🚀 快速开始模板

### 新任务模板

为团队成员提供任务模板：

```bash
# 创建新任务目录
mkdir -p tasks/my_new_task/mdp

# 创建必要的文件
touch tasks/my_new_task/__init__.py
touch tasks/my_new_task/my_new_task_env_cfg.py
touch tasks/my_new_task/mdp/__init__.py
touch tasks/my_new_task/README.md
```

### 任务模板代码

**`tasks/my_new_task/__init__.py`**：

```python
import gymnasium as gym

gym.register(
    id="Isaac-MyNewTask-v0",
    entry_point="isaaclab.envs:ManagerBasedRLEnv",
    disable_env_checker=True,
    kwargs={
        "env_cfg_entry_point": f"{__name__}.my_new_task_env_cfg:MyNewTaskEnvCfg",
    },
)
```

**`tasks/my_new_task/my_new_task_env_cfg.py`**：

```python
from isaaclab.envs import ManagerBasedRLEnvCfg
from isaaclab.utils import configclass

@configclass
class MyNewTaskEnvCfg(ManagerBasedRLEnvCfg):
    """新任务配置"""
    
    # TODO: 实现场景、观察、动作、奖励等配置
    
    def __post_init__(self):
        self.decimation = 2
        self.episode_length_s = 5.0
```

---

## 📋 协作检查清单

### 提交前检查

- [ ] 任务可以正确注册（`gym.make()` 能创建环境）
- [ ] 使用统一入口脚本可以运行
- [ ] 如果有相机，视频录制功能正常
- [ ] 代码符合项目规范
- [ ] 添加了必要的文档
- [ ] 没有修改共享文件（除非是公共改进）

### Pull Request 检查

- [ ] 任务名称不冲突
- [ ] 代码风格一致
- [ ] 通过了所有测试
- [ ] 文档完整

---

## 🎯 总结

### 协作优势

✅ **完全解耦**：每个任务独立，互不影响  
✅ **自动化**：无需手动注册，自动发现  
✅ **统一接口**：所有任务使用相同的 API  
✅ **易于扩展**：添加新任务只需创建目录  
✅ **并行开发**：多人可以同时开发不同任务  

### 核心原则

1. **最小化共享代码修改**：每个人只修改自己的任务目录
2. **遵循规范**：使用标准的目录结构和命名规范
3. **文档完善**：每个任务都有清晰的文档
4. **测试充分**：提交前确保任务可以正常运行

### 工作流程

```
1. 克隆仓库
   ↓
2. 创建功能分支
   ↓
3. 开发自己的任务（在独立目录中）
   ↓
4. 本地测试（使用统一入口脚本）
   ↓
5. 提交 Pull Request
   ↓
6. 代码审查和合并
   ↓
7. 所有人拉取更新，任务自动可用
```

---

## 🔗 相关文档

- [统一入口脚本使用说明.md](./统一入口脚本使用说明.md)
- [Realman_Lift任务嵌入与调用详解.md](./Realman_Lift任务嵌入与调用详解.md)

---

**只要遵循规范，多人协作开发会非常顺畅！** 🚀

